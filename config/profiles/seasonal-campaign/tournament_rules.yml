defaults:
  include: false
  weight: 0.75
  tags: []

rules:
  - name: "Include Trackmania seasonal campaign events"
    match:
      series: "Trackmania_Seasonal_Campaign"
    include: true
    weight: 0.75
    tags:
      - campaign
